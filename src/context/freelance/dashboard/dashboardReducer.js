// import {
// 	FETCH_ALL_USERS,
// } from "../../../service/admin/adminAction";

const dashboardReducer = (state, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export { dashboardReducer };
